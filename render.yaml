services:
  - type: web
    name: retenciones-facturas-colombia
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: HOST=0.0.0.0 PYTHONPATH=src python -m cifrato_retenciones.web
